import { createpatelConfig } from "patel-agent-playwright-config/config";

export default createpatelConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
